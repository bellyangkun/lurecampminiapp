page { background: #fff8e1; }
.placeholder { display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100vh; }
.emoji { font-size: 96rpx; }
.title { font-size: 36rpx; font-weight: 700; margin-top: 24rpx; color: #FF6F00; }
.hint { font-size: 24rpx; color: #999; margin-top: 12rpx; }
